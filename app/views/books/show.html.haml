.row
  .col-md-2
  .col-md-8
    %h1
      = @book.title
      %small by #{@book.author}

    %img.cover{ src: @book.image_url }

    %p= @book.description

    - unless current_user.reviewed_book?(@book)
      %button.btn.btn-default.add-review
        Add Review
    - if current_user
      %button.btn.btn-default.make-recommendation
        Recommend to a Friend

    -#  Reviews
    %section.reviews
      %h4.text-center Reviews
      - @book.reviews.each do |review|
        .panel.panel-default
          .panel-heading.clearfix
            %h6
              %strong= review.user.email
              %span.pull-right #{review.rating} / 5
          .panel-body
            %p= review.text
    -# Review Form
    %form.add-review-form{ 'data-id' => @book.id, 'ng-controller' => 'reviewFormCtrl as reviewForm' }
      %h6.pull-left New Review
      %rating.pull-right(ng-model="rate" max="max" on-hover="hoveringOver(value)" on-leave="overStar = null")
      %span.label.pull-right(ng-class='{"label-warning": overStar<=2, "label-info": overStar>2 && overStar<4, "label-success": overStar>=4}' ng-show='overStar')= '{{overStar}}'
      %input.form-control(type= "hidden" name="review[rating]" ng-value='rate')
      %textarea.form-control{ name: 'review[text]', placeholder: 'Review', rows: 10 }
      %input.btn.btn-default{ type: 'submit', value: 'Save' }
    %hr
  .col-md-2



-# Recommendation Modal
- if current_user
  .recommendation-modal.modal.fade
    .modal-dialog
      .modal-content
        .modal-header
          %button.close{"data-dismiss" => "modal", :type => "button"}
            %span{"aria-hidden" => "true"} &times;
            %span.sr-only Close
          %h4.modal-title Recommend to a Friend
        .modal-body
          %form
            %select{ name: 'recommendation[recipient_id]' }
              - current_user.messagable_friends.each do |u|
                %option{ value: u.id }= u.email
            %textarea.form-control{ name: 'recommendation[text]', placeholder: 'Include a message', rows: 10 }
        .modal-footer
          %button.btn.btn-default{"data-dismiss" => "modal", :type => "button"} Close
          %button.btn.btn-primary.submit Send


